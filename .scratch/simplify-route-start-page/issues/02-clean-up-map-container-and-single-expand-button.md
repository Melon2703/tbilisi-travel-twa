# 02 — Clean Up Map Container Header and Keep Single Full-Screen Expand Trigger

**What to build:** Simplify the map view on the route overview page by removing the redundant internal card header ("Route overview map", "6 stops", etc.) and removing the duplicate "Expand full map" overlay pill from inside the map canvas, leaving only one clean full-screen expand button.

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] Redundant header bar inside map container ("Route overview map", "6 stops") is removed so the map fills the container width cleanly.
- [ ] Duplicate "Expand full map" overlay button inside map view is removed, keeping strictly one expand button on the map section.
- [ ] Click interaction opens the fullscreen map modal without visual duplication.
